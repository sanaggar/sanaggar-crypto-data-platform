# =============================================================================
# CRYPTO DATA PLATFORM - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git!
# =============================================================================

# PostgreSQL (required)
POSTGRES_USER=crypto_admin
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=crypto_data

# Airflow (required)
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=your_airflow_password_here

# Grafana (required)
GRAFANA_ADMIN_PASSWORD=your_grafana_password_here

# --- Optional below ---

# Airflow Fernet key (leave empty to auto-generate)
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#AIRFLOW_FERNET_KEY=

# GitHub (optional - for Airflow DAG sync from remote repo)
# If not set, local DAGs from the dags/ folder will be copied into Airflow
#GITHUB_USERNAME=
#GITHUB_TOKEN=
